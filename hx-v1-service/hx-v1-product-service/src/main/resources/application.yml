server:
  port: 8081
dubbo:
  application:
    name: product-service-privter
  registry:
    address: zookeeper://127.0.0.1:2181
  protocol: 28001
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/product_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root